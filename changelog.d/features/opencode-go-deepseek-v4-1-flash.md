- **feat(providers):** add DeepSeek V4.1 Flash to OpenCode Go and Command Code, plus 13 additional OpenCode Go base models from its public catalog. OpenCode Go declares V4.1's 1M context, 384K output limit, vision capability and `none`/`low`/`high`/`max` effort options. V4.1 and Flash Vision Exp use the existing DeepSeek alias rewrite to base ids with `reasoning_effort`. New non-DeepSeek models expose base ids with their configured wire formats; translating their local effort aliases is a separate change. Command Code uses its existing DeepSeek effort vocabulary and output limit; its public discovery confirms V4.1's id and 1M context.
