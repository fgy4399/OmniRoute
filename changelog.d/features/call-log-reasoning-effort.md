- **feat(logs):** show the reasoning effort actually captured on outbound provider requests in the request-log table, details and BigQuery exports. The summary is captured independently of detailed body logging, so retries that remove effort clear the value, and requests without a captured dispatch or served from semantic cache record NULL. Migration 176 adds the nullable `reasoning_effort` column with runtime schema repair; existing BigQuery tables gain the nullable field when automatic schema management is enabled. Effort carriers include Chat Completions, Responses and Anthropic output configuration; explicit thinking-off records `none`, while budget-only requests do not invent a tier.
